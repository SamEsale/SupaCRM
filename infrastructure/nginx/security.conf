server_tokens off;
client_max_body_size 20m;
limit_req_zone $binary_remote_addr zone=supacrm_auth_ip:10m rate=10r/m;
limit_req_zone $binary_remote_addr zone=supacrm_api_ip:10m rate=120r/m;

map $request_uri $supacrm_block_bad_uri {
    default 0;
    ~*\.\./ 1;
    ~*%2e%2e 1;
    ~*%5c 1;
}
